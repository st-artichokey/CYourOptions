# Session Summary — 2026-04-29

CSV as source of truth, MAUI removal, framework consolidation, and Godot improvements.

---

## lint-csv Skill

### Created lint-csv skill for story CSV validation

Added a `/lint-csv` skill (modeled after `/playtest`) that validates story CSV files for structural errors, graph integrity, narrative quality, and pacing issues.

**Changes:**
- `.claude/skills/lint-csv/SKILL.md` — skill definition
- `CYourOptions/.claude/skills/lint-csv/SKILL.md` — project-level copy

**Refinements after feedback:**
- Removed editorializing — reports facts only, no commentary or fix suggestions
- Changed longest-path from a warning to an informational note in summary (8-node threshold was arbitrary)

---

## CSV as Runtime Source of Truth

### Remove generated story class; load CSV at runtime

Replaced the 350-line generated `TheProductionIncident.cs` with runtime CSV loading. Each platform uses its native asset API to load story data.

**Changes (PR #3, merged):**
- Deleted `src/CYourOptions.Library/Stories/TheProductionIncident.cs`
- Added `CsvStoryLoader.LoadEmbedded()` (later removed in favor of per-platform loading)
- MAUI: loads from `Resources/Raw/` via `FileSystem.OpenAppPackageFileAsync()`
- Godot: loads from `res://` via `FileAccess.Open()`
- Tests: load from `tools/CsvStoryGenerator/examples/` via `CsvStoryLoader.LoadFromCsv()`
- Updated story content with new nodes (make_coffee, flip_it_yourself, validate_after_skip, ending_bandaid_test — 31 nodes, 7 endings)

---

## Remove MAUI, Consolidate on Godot

### PR #4: Remove MAUI app entirely

Godot covers all platforms (desktop, mobile, web) with a simpler build pipeline. MAUI added Xcode issues and platform boilerplate without benefit for a game-style UI.

**Changes:**
- Deleted entire `src/CYourOptions.App/` directory (1,212 lines)
- Updated `CYourOptions.slnx` to remove MAUI project reference

---

## Framework Consolidation

### Standardize on net8.0

Installed .NET 8 SDK, set it as default in `~/.zshrc`, and dropped net10.0 multi-targeting.

**Changes:**
- `src/CYourOptions.Library/CYourOptions.Library.csproj` — `net8.0` single target
- `src/CYourOptions.Tests/CYourOptions.Tests.csproj` — `net8.0`
- `tools/CsvStoryGenerator/CsvStoryGenerator.csproj` — `net8.0`
- `~/.zshrc` — added `/opt/homebrew/opt/dotnet@8/bin` to PATH

---

## Dead Code Removal

### Remove unused code identified by review

- Deleted `InputAction.cs` — enum never referenced anywhere
- Removed `SaveManager` field from `GameController.cs` — instantiated but never called

---

## Godot Bug Fixes & Multi-Story Support

### Fix bugs and add story folder structure

**Bug fixes:**
- Added null checks on `FileAccess.Open()` with diagnostic error messages via `FileAccess.GetOpenError()`
- Fixed choice button disambiguation: capture loop index directly instead of resolving `NextNodeId` via `FindIndex`

**Multi-story support:**
- Added `[Export] StoryPath` property — configurable in Godot inspector
- Start node derived from CSV (first row) instead of hardcoded constant
- Stories live in `res://stories/<story-name>/` folder structure
- To add a story: create folder, drop `nodes.csv` + `choices.csv`, set path in inspector

**Changes:**
- `src/CYourOptions.Game/Scripts/GameController.cs` — rewritten with fixes
- `src/CYourOptions.Game/stories/the-production-incident/nodes.csv` — moved from project root
- `src/CYourOptions.Game/stories/the-production-incident/choices.csv` — moved from project root

---

## Current State

- **Branch:** `refactor/remove-maui` (PR #4 open)
- **Tests:** 36 passing on net8.0
- **Architecture:** Library (engine + parser) → Godot (single frontend) → CSV stories (game content)
- **Story tool:** `tools/CsvStoryGenerator/` kept with examples for documentation/reference
